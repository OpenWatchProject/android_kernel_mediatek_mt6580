Blocks Sombrero Kernel
===========

To build sombrero kernel without AOSP, use the following script:

    ./build-boot-image.sh -j8

After the building, you can find the built boot image in `bootimg/boot.img`

